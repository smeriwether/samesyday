{
  "name": "samesyday",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build:css",
    "build:css": "tailwindcss -i src/styles.css -o public/assets/styles.css --minify",
    "test": "node --test",
    "check": "npm test && npm run build",
    "dev:css": "tailwindcss -i src/styles.css -o public/assets/styles.css --watch",
    "preview": "wrangler pages dev public"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.18",
    "tailwindcss": "^4",
    "wrangler": "^4.48.0"
  }
}
